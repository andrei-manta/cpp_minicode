#include <string>

int main()
{
        //the length of an emty string is 0
        std::string s1;

        //the length of a STRING LITERAL is number of chars + NULL TERMINATOR,
        //so "" has length 1, but s2 has length 0
        std::string s2 = "";

        return 0;
}